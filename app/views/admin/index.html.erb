<h1>Users (Not students)</h1>
<ul>
<% @users.each do |user| %>
  <li>
    <%= link_to "User id #{user.id}", admin_user_path(user) %>
    <%= render partial: 'admin/shared/user', locals: {user: user} %>
  </li>
<% end %>
</ul>
