<h3>Year of Graduation: <%= @cohort.graduation_year %></h3>
<%= link_to 'Add new student', new_student_path %>
<ul>
<% @cohort.students.each do |student| %>
  <li><%= link_to student.name, student_path(student) %></li>
<% end %>
</ul>
