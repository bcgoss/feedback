<%= form_for student do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :phone_number %>
  <%= f.text_field :phone_number %><br>
  <%= f.label 'Class of ' %>
  <%= f.collection_select :cohort_id, Cohort.all, :id, :graduation_year %><br>
  <%= f.submit %>
<% end %>
